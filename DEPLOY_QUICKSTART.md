# 🚀 Быстрый старт: Деплой в Railway

## 📦 Что нужно сделать

### 1. Подготовка (5 минут)

```bash
# Убедитесь, что все закоммичено
git add .
git commit -m "Prepare for Railway deployment"
git push
```

### 2. Создание проекта в Railway (10 минут)

1. **Зайдите на [railway.app](https://railway.app)** и войдите через GitHub

2. **Создайте новый проект:**
   - Нажмите "New Project"
   - Выберите "Deploy from GitHub repo"
   - Выберите ваш репозиторий

3. **Добавьте PostgreSQL:**
   - Нажмите "+ New" → "Database" → "Add PostgreSQL"
   - Скопируйте `DATABASE_URL` из Variables

### 3. Деплой Backend (5 минут)

1. **Создайте новый сервис:**
   - Нажмите "+ New" → "GitHub Repo"
   - Выберите ваш репозиторий

2. **Настройте сервис:**
   - **Settings** → **Root Directory**: `backend`
   - **Settings** → **Build Command**: `npm install && npm run build`
   - **Settings** → **Start Command**: `npm run db:push && npm start`

3. **Добавьте переменные окружения (Variables):**
   ```
   DATABASE_URL=<скопируйте из PostgreSQL сервиса>
   JWT_SECRET=<сгенерируйте: openssl rand -base64 32>
   PORT=3001
   ROUND_DURATION=60
   COOLDOWN_DURATION=30
   FRONTEND_URL=<пока оставьте пустым, заполните после деплоя frontend>
   NODE_ENV=production
   ```

4. **Сгенерируйте домен:**
   - Settings → Networking → Generate Domain
   - Скопируйте URL (например: `https://backend-production-xxxx.up.railway.app`)

### 4. Деплой Frontend (5 минут)

1. **Создайте новый сервис:**
   - Нажмите "+ New" → "GitHub Repo"
   - Выберите ваш репозиторий

2. **Настройте сервис:**
   - **Settings** → **Root Directory**: `frontend`
   - **Settings** → **Dockerfile Path**: `Dockerfile` (или оставьте пустым, Railway найдет автоматически)
   - **Settings** → **Build Command**: (оставьте пустым, используется Dockerfile)
   - **Settings** → **Start Command**: (оставьте пустым, nginx запускается автоматически через Dockerfile)

3. **Добавьте переменные окружения:**
   ```
   VITE_API_URL=<URL вашего backend, например: https://backend-production-xxxx.up.railway.app>
   NODE_ENV=production
   PORT=80
   ```
   
   **Важно:** 
   - `VITE_API_URL` должен быть полным URL вашего backend сервиса (например: `https://backend-production-xxxx.up.railway.app`). Путь `/api` будет добавлен автоматически.
   - `PORT=80` - Railway автоматически установит эту переменную, но можно указать явно для надежности

4. **Сгенерируйте домен:**
   - Settings → Networking → Generate Domain
   - Скопируйте URL (например: `https://frontend-production-xxxx.up.railway.app`)

### 5. Финальная настройка (2 минуты)

1. **Обновите FRONTEND_URL в Backend:**
   - Зайдите в Backend сервис → Variables
   - Обновите `FRONTEND_URL` на URL вашего frontend
   - Сервис автоматически перезапустится

2. **Проверьте работу:**
   - Откройте URL frontend в браузере
   - Проверьте, что приложение загружается
   - Попробуйте зарегистрироваться/войти

## ✅ Готово!

Теперь ваше приложение доступно по URL, который Railway сгенерировал для frontend.

## 🔧 Если что-то не работает

### Backend не запускается
- Проверьте логи в Railway Dashboard
- Убедитесь, что `DATABASE_URL` правильный
- Проверьте, что PostgreSQL сервис запущен

### Frontend не подключается к Backend
- Проверьте `VITE_API_URL` во frontend
- Проверьте `FRONTEND_URL` в backend (должен быть URL frontend)
- Убедитесь, что оба сервиса запущены

### Ошибки сборки
- Проверьте логи сборки
- Убедитесь, что все файлы закоммичены
- Проверьте, что Root Directory указан правильно

## 📝 Важные переменные

**Backend:**
- `DATABASE_URL` - обязательно
- `JWT_SECRET` - обязательно (минимум 32 символа)
- `FRONTEND_URL` - обязательно (URL вашего frontend)

**Frontend:**
- `VITE_API_URL` - обязательно (URL вашего backend)

## 🎯 Следующие шаги

- Настройте кастомный домен (опционально)
- Настройте автоматический деплой из main ветки
- Добавьте мониторинг и алерты

