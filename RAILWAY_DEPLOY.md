# 🚂 Сценарий деплоя проекта в Railway

Этот документ описывает пошаговый процесс деплоя проекта "The Last of Guss" в Railway.

## 📋 Структура проекта

Проект состоит из:
- **Backend** (Fastify + TypeScript + PostgreSQL)
- **Frontend** (React + Vite + TypeScript)
- **PostgreSQL** база данных

## 🎯 Вариант 1: Деплой через Railway UI (Рекомендуется)

### Шаг 1: Подготовка репозитория

1. Убедитесь, что все изменения закоммичены и запушены в GitHub/GitLab/Bitbucket
2. Проверьте, что есть файлы:
   - `backend/Dockerfile`
   - `frontend/Dockerfile`
   - `backend/package.json`
   - `frontend/package.json`

### Шаг 2: Создание проекта в Railway

1. Зайдите на [railway.app](https://railway.app)
2. Войдите через GitHub/GitLab
3. Нажмите **"New Project"**
4. Выберите **"Deploy from GitHub repo"**
5. Выберите ваш репозиторий

### Шаг 3: Добавление PostgreSQL базы данных

1. В проекте Railway нажмите **"+ New"**
2. Выберите **"Database"** → **"Add PostgreSQL"**
3. Railway автоматически создаст базу данных
4. **Важно**: Запишите переменные окружения из вкладки **"Variables"**:
   - `PGHOST`
   - `PGPORT`
   - `PGUSER`
   - `PGPASSWORD`
   - `PGDATABASE`
   - `DATABASE_URL` (готовая строка подключения)

### Шаг 4: Деплой Backend

1. В проекте Railway нажмите **"+ New"** → **"GitHub Repo"**
2. Выберите тот же репозиторий
3. В настройках сервиса:
   - **Root Directory**: `backend`
   - **Build Command**: `npm install && npm run build`
   - **Start Command**: `npm run db:push && npm start`
   - **Watch Paths**: `backend/**`

4. Настройте **Variables** (Environment Variables):
   ```
   DATABASE_URL=<значение из PostgreSQL сервиса>
   JWT_SECRET=<сгенерируйте случайную строку, минимум 32 символа>
   PORT=3001
   ROUND_DURATION=60
   COOLDOWN_DURATION=30
   FRONTEND_URL=<URL вашего фронтенда, будет настроен позже>
   NODE_ENV=production
   ```

5. Настройте **Settings**:
   - **Healthcheck Path**: `/health`
   - **Port**: `3001`

### Шаг 5: Деплой Frontend

1. В проекте Railway нажмите **"+ New"** → **"GitHub Repo"**
2. Выберите тот же репозиторий
3. В настройках сервиса:
   - **Root Directory**: `frontend`
   - **Dockerfile Path**: `Dockerfile` (Railway автоматически обнаружит его)
   - **Build Command**: (оставьте пустым, сборка выполняется через Dockerfile)
   - **Start Command**: (оставьте пустым, nginx запускается автоматически через Dockerfile)
   - **Watch Paths**: `frontend/**`

4. Настройте **Variables**:
   ```
   VITE_API_URL=<URL вашего backend сервиса>
   NODE_ENV=production
   ```

5. **Важно**: После деплоя backend, скопируйте его URL и:
   - Обновите `FRONTEND_URL` в backend сервисе на URL фронтенда
   - Обновите `VITE_API_URL` во frontend сервисе на URL backend

### Шаг 6: Настройка доменов

1. В каждом сервисе (backend и frontend) перейдите в **Settings** → **Networking**
2. Нажмите **"Generate Domain"** для каждого сервиса
3. Скопируйте полученные URL
4. Обновите переменные окружения:
   - В **Backend**: `FRONTEND_URL=https://your-frontend-domain.railway.app`
   - В **Frontend**: `VITE_API_URL=https://your-backend-domain.railway.app`
5. Перезапустите сервисы после изменения переменных

## 🐳 Вариант 2: Деплой через Dockerfile

Railway автоматически обнаружит Dockerfile в корне проекта или в указанной директории.

### Backend (уже есть `backend/Dockerfile`)

Railway автоматически использует `backend/Dockerfile` если указать **Root Directory**: `backend`

### Frontend (создан `frontend/Dockerfile`)

Railway автоматически использует `frontend/Dockerfile` если указать **Root Directory**: `frontend`

## 📝 Важные замечания

### Переменные окружения

**Backend требует:**
- `DATABASE_URL` - строка подключения к PostgreSQL
- `JWT_SECRET` - секретный ключ для JWT (минимум 32 символа)
- `PORT` - порт сервера (Railway автоматически устанавливает через `$PORT`)
- `FRONTEND_URL` - URL фронтенда для CORS
- `ROUND_DURATION` - длительность раунда в секундах (опционально, по умолчанию 60)
- `COOLDOWN_DURATION` - длительность кулдауна в секундах (опционально, по умолчанию 30)

**Frontend требует:**
- `VITE_API_URL` - URL backend API (должен начинаться с `https://`)

### Порты

Railway автоматически устанавливает переменную `$PORT`. Если ваш код использует `process.env.PORT`, он автоматически получит правильный порт.

### Healthcheck

Backend имеет endpoint `/health` для проверки работоспособности. Railway автоматически использует его для healthcheck.

### Миграции базы данных

Backend автоматически применяет миграции при старте через команду `npm run db:push` в Dockerfile.

## 🔧 Troubleshooting

### Backend не запускается

1. Проверьте логи в Railway Dashboard
2. Убедитесь, что `DATABASE_URL` правильно настроен
3. Проверьте, что база данных доступна (проверьте статус PostgreSQL сервиса)

### Frontend не может подключиться к Backend

1. Убедитесь, что `VITE_API_URL` правильно настроен
2. Проверьте CORS настройки в backend (должен быть указан правильный `FRONTEND_URL`)
3. Проверьте, что backend сервис запущен и доступен

### Ошибки сборки

1. Проверьте логи сборки в Railway Dashboard
2. Убедитесь, что все зависимости указаны в `package.json`
3. Для frontend: убедитесь, что `VITE_API_URL` доступен во время сборки (может потребоваться использовать переменные окружения Railway)

## 📚 Полезные ссылки

- [Railway Documentation](https://docs.railway.app)
- [Railway Environment Variables](https://docs.railway.app/develop/variables)
- [Railway Networking](https://docs.railway.app/develop/networking)

## ✅ Чеклист деплоя

- [ ] Репозиторий подключен к Railway
- [ ] PostgreSQL база данных создана
- [ ] Backend сервис создан и настроен
- [ ] Frontend сервис создан и настроен
- [ ] Все переменные окружения установлены
- [ ] Домены сгенерированы для обоих сервисов
- [ ] `FRONTEND_URL` в backend указывает на URL фронтенда
- [ ] `VITE_API_URL` во frontend указывает на URL backend
- [ ] Оба сервиса успешно деплоятся
- [ ] Healthcheck проходит успешно
- [ ] Приложение работает и доступно по доменам

